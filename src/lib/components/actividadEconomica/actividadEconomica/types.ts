/** One CIIU4 economic-activity code from Hacienda’s correspondence catalog. */
export type ActividadEconomicaEntry = {
	/** Código subclase TRIBU-CR / FE (e.g. `6201.0`). */
	codigo: string;
	descripcion: string;
};
